\begin{figure}
   \resizebox{\textwidth}{!}{%
        \input{tikz/P_map_GL.tex}
}
    \caption{On the left hand side we show a map of neighboring counties $r$ to Gütersloh county, with associated $p_{\text{GL},r}$ (in $\%$) indicating how our model distributes secondary cases infected from primary cases in Gütersloh to neighboring counties. 
The right-hand side shows the corresponding $q_{\text{GL}, r}$ values. As $p_{\text{GL}, \text{GL}}$ and $q_{\text{GL}, \text{GL}}$ are much larger than the other values, we apply the red colorscheme only to the other counties. Notice that the seven direct neighboring counties are also the counties with the highest values in both figures.}
    \label{fig:P_map_GL}
\end{figure}
