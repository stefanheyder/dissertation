\begin{figure}
   \resizebox{\textwidth}{!}{%
        \input{tikz/hospitalization_showcase_results_states.tex}
}
    \caption{For each of the seven age groups (indicated by colour and linetype) we show means of the smoothing distribution for the hospitalization probability $p_t$ (top), the weekday effects $W_{t,0}$ and $W_{t,1}$ (middle) and the average delay $\bar{\\tau}_t$ (bottom). Recall that we fit a separate model for each age group. For the smoothed hospitalization probabilities $p_t$ we additionally show 95\% prediction intervals. Note the log-scale of the y-axis for $p_t$.}
    \label{fig:hospitalization_showcase_results_states}
\end{figure}
